## Network-Port-Scan-Detector

A script using Python and the “dpkt” library to identify potentially malicious scanning patterns, including Null, Xmas, UDP, Half-Open, and Connect scan types. 

Run in terminal using: "python detector.py -i <pcap file>.pcap"
